###################################################
# Services
###################################################
services:

  ###################################################
  # Service: backend
  ###################################################
  backend:
    ports:
      - 8182:8188
    image: zinigor/comfy:0.6
    build:
      context: ./
      target: backend-dev
    environment:
      __COMFYUI_DESKTOP_VERSION__: 0.3.39
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
